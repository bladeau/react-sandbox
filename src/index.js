import React from "react";
import ReactDOM from "react-dom";

//import App from './App-Complete-Guide/App';
import App from "./Discover-Functional-JS/App";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  rootElement
);
